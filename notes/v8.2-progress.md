# ccteam v8.2 Progress

Resume: read this file + git state only; do not re-read full plan.html/im.html.

## Status

- Q1 real path: partial. `scripts/smoke-im.sh --real` now fail-louds instead
  of fake PASS; checks real claude/codex/tmux + Codex app-server socket. Local
  run exits 2: missing `/home/rob/.codex/app-server-control/app-server-control.sock`.
  Still pending: true init->start->pair->message->reply and Telegram evidence.
- Q2 multi-project x multi-session routing: fake covered. Gateway test now
  proves one chat across 2 projects x 4 sessions plus `@handle`, `/cd`,
  `/sessions`, `/projects`, and cross-chat isolation. Real-path proof pending.
- Q3 dual harness concurrency: fake covered. Default Codex adapter is now
  `CodexAppServerAdapter`; gateway sends Codex `/compact`/`/review` as
  `SystemDirective`; Claude slash commands pass through. Real proof pending.
- Q4 durability: pending. Kill/restart daemon mid-chat; IM transport reconnects,
  outbound persists, Claude re-attaches, Codex resumes, same session continues.
- Q5 Telegram + failures: pending. Long connection plus visible errors/recovery.
- Q6 cleanup/gates: pending. Remove dead tick code only after no prod callers;
  build workspace + harness, workspace tests >=1732, clippy, fmt, smoke normal
  and `--real`, docs/version sync.

## Redlines

HarnessAdapter x ProcessBackend two axes; pane ops only in PaneBackend;
CanonicalEvent/ApprovalIR neutral; daemon no tick; skip-permissions no approval;
`ccteam-im` no `ccteam-flow`; progress single SoT; topology `core -> harness`;
new transport as Channel impl.
