# ccteam v8.2 Progress

Resume: read this file + git state only; do not re-read full plan.html/im.html.

## Status

- Q1 real path: partial. `--real` fail-louds; npm Codex uses real `stdio://`.
  `CCTEAM_REAL_CODEX_RPC=1` passes `thread/start`; `CCTEAM_REAL_IM_WS=1`
  passes WS dual-harness command smoke; `CCTEAM_REAL_IM_WS_NL=1` proves true
  Codex+Claude replies over WS; opt-in Telegram roundtrip gate added. Pending: live Telegram evidence.
- Q2 multi-project x multi-session routing: fake covered. Gateway test now
  proves one chat across 2 projects x 4 sessions plus `@handle`, `/cd`,
  `/sessions`, `/projects`, and cross-chat isolation. WS proof added.
- Q3 dual harness concurrency: fake covered. Real WS creates Codex+Claude, routes
  Codex `/compact`, Claude `/clear`, and async CanonicalEvent replies from both.
- Q4 durability: partial. Fake + real WS restart reconnect same chat, reuse `s1/s2`,
  resume Claude tmux + Codex app-server, and replay outbound. Pending: Telegram faults.
- Q5 Telegram + failures: partial. Start/submit/turn-timeout + real Claude tmux
  death + Codex app-server disconnect visible in IM+ledger; real fault smoke green. Pending: live Telegram.
- Q6 cleanup/gates: done for WS path. Removed dead tick_supervisors/DaemonArgs.tick;
  version `0.8.2`; gates green: workspace build, harness build, tests 1743,
  clippy, fmt, smoke normal + real WS fault. Pending only Telegram evidence.

## Redlines

HarnessAdapter x ProcessBackend two axes; pane ops only in PaneBackend;
CanonicalEvent/ApprovalIR neutral; daemon no tick; skip-permissions no approval;
`ccteam-im` no `ccteam-flow`; progress single SoT; topology `core -> harness`;
new transport as Channel impl.
