[package]
name = "ccteam-hooks"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "ccteam hook handlers (progress-append, load-context, intercept-ask)."

[dependencies]
ccteam-core.workspace = true
ccteam-harness.workspace = true
serde.workspace = true
serde_json.workspace = true
chrono.workspace = true
tracing.workspace = true
anyhow.workspace = true

[dev-dependencies]
tempfile = "3"
# V0.6.0 F108 — chat_progress integration tests share env vars
# (`CCTEAM_CHAT_ROLE`, `CCTEAM_HOME`) and must serialize on the same
# process-wide mutex to avoid cross-test bleed.
serial_test = "3"
