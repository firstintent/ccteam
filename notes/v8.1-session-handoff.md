# v8.1 Session Handoff

## Current State

- Branch: `dev`
- Worktree: clean after commit `d76771d`
- No PR opened, no push performed.
- CodeGraph status during session: initialized; no indexed `crates/ccteam-imd` files found.

## Key Commits Created

- `79b8021 Update rmux smoke script for harness crate`
- `e8edf0e Move Claude TUI adapter into ccteam-harness`
- `4b91510 Move Codex exec adapter into ccteam-harness`
- `d76771d Move Codex app-server adapter into ccteam-harness`

Recent prior commits already present before this handoff:

- `4e9093e Extract workflow orchestration into ccteam-flow`
- `9764480 Separate pane backend selection from process backend`

## Plan Progress

### P0: mux -> harness

Done.

Evidence:

- `crates/ccteam-harness` exists and builds.
- `scripts/rmux-smoke.sh` now targets `ccteam-harness`.
- `grep -R "ccteam-mux\|ccteam_mux\|MuxBackend" -n crates scripts Cargo.toml` returned no matches.

### P1: execution layer out of fat core

Mostly done for execution adapters and flow split.

Evidence:

- `orchestrator`, `workflow`, `artifact_watcher`, and watcher tests live in `ccteam-flow`.
- `grep -n 'pub mod orchestrator' crates/ccteam-core/src/lib.rs` returned no matches.
- `cargo tree -p ccteam-im --prefix none | grep -i 'ccteam-flow'` returned no matches.
- Concrete adapters moved into `ccteam-harness/src/execution/`:
  - `claude_bg.rs`
  - `claude_tui.rs`
  - `codex_exec.rs`
  - `codex_app_server.rs`
  - `typed_events.rs`
  - `codex_typed_events.rs`
- `crates/ccteam-core/src/execution/mod.rs` is now a compatibility re-export layer only.

Remaining nuance:

- `ccteam-core` still re-exports adapter modules/types for compatibility. This keeps existing call sites compiling, but a future cleanup can remove the compatibility layer if the project wants stricter primitives-only enforcement.

### P2: Claude adapter in harness

Done.

Evidence:

- `ClaudeTuiAdapter` source and `claude_tui_*` tests moved to `ccteam-harness`.
- `cargo test -p ccteam-harness claude_tui --locked` passed.
- Explicit migrated test targets passed:
  - `claude_tui_test`
  - `claude_tui_env_test`
  - `claude_tui_fanout_test`
  - `claude_tui_reattach_test`
  - `claude_tui_resume_test`
  - `claude_tui_silence_warn_test`
- `ClaudeTuiAdapter` still launches `claude --dangerously-skip-permissions`.

### P3: Codex adapters in harness

Done for existing `codex exec` and `codex app-server` adapters.

Evidence:

- `CodexExecAdapter`, `CodexAppServerAdapter`, and Codex typed-event producer moved to `ccteam-harness`.
- `cargo test -p ccteam-harness codex_app_server --locked` passed.
- Explicit app-server targets passed:
  - `cargo test -p ccteam-harness --test codex_app_server_test --test codex_typed_events_test --locked`
- `cargo test -p ccteam-harness --test codex_exec_wave3_test --locked` passed.
- `cargo test -p ccteam-core --test codex_app_server_progress_bridge_test --locked` passed.

Remaining nuance:

- `CodexAppServerAdapter` currently connects to an existing app-server socket. The original plan says spawn `codex app-server`; current implementation preserves the pre-existing adapter behavior. Decide whether P3 should require spawn-on-demand before marking P3 fully complete.
- `/compact` and `/review` RPC command mapping should be explicitly audited next. `TurnInput::SystemDirective` is currently rejected in app-server tests, so plan acceptance for command RPC is not fully proven.

## Validations Run

Passed:

- `cargo build --workspace --locked`
- `cargo build -p ccteam-cli --locked`
- `cargo test -p ccteam-harness --locked`
- `cargo test -p ccteam-harness claude_tui --locked`
- `cargo test -p ccteam-harness codex_app_server --locked`
- `cargo test -p ccteam-harness --test codex_app_server_test --test codex_typed_events_test --locked`
- `cargo test -p ccteam-harness --test codex_exec_wave3_test --locked`
- `cargo test -p ccteam-core --test harness_trait_test --test codex_critic_ledger_test --locked`
- `cargo test -p ccteam-core --test codex_app_server_progress_bridge_test --test typed_event_pipeline_test --locked`
- `cargo fmt --all -- --check`
- `git diff --check`

Not run yet this session:

- Full baseline: `cargo test --workspace --locked --no-fail-fast --exclude ccteam-web`
- Clippy: `cargo clippy --workspace --all-targets -- -D warnings`
- P5 smoke: `bash scripts/smoke-im.sh` does not exist yet.

## Next Work

Start with P3/P4 boundary audit before writing more code:

- Confirm whether `CodexAppServerAdapter` must spawn `codex app-server` on demand or whether socket-only is acceptable for this phase.
- Implement/verify command RPC mapping for Codex app-server:
  - `/compact` -> `thread/compact/start`
  - `/review` -> `review/start`
  - `resume_thread` -> `thread/resume`
- Then continue P4:
  - `ccteam-im` gateway route table for `chat -> project -> session`
  - commands `/use`, `/cd`, `/new <vendor> <role>`, `/sessions`, `/projects`
  - fake transport + fake harness tests: `cargo test -p ccteam-im gateway`

## Compression Prompt

Continue the v8.1 work in `/home/rob/workplace/agents/ccteam` on branch `dev`.

Read first:

1. `CLAUDE.md`
2. `notes/plan.html`
3. `notes/im.html`
4. `notes/v8.1-session-handoff.md`

Do not reset, do not delete untracked files, do not push, do not open a PR.

Current last commit at handoff: `d76771d Move Codex app-server adapter into ccteam-harness`.

Resume by auditing P3 remaining command/spawn requirements, then continue P4 IM gateway implementation.
