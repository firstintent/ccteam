# Connection config for `make notes-push` / `notes-pull`.
# Copy to .env (gitignored) and fill in your real host — do NOT commit .env.
#
#   cp .env.example .env   &&   $EDITOR .env
NOTES_HOST=user@your.remote.host
NOTES_REMOTE=/home/user/workplace/ccteam/notes
NOTES_KEY=~/.ssh/id_ed25519
